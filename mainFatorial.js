const fat = require('./fatorial')

const num = 7
console.log(`
Program for factorial of a number
---------------------------------
`)
console.log(`
O fatorial do número ${num} é => ${fat.fat(num)}
---------------------------------
`)
